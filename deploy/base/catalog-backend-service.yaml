---
apiVersion: v1
kind: Service
metadata:
  name: catalog-backend
  labels:
    app: catalog-backend
    fdk.service: catalog-backend
spec:
  selector:
    fdk.service: catalog-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
