services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: catalog_backend
      POSTGRES_USER: root
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
